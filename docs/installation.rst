.. highlight:: shell

============
Installation
============


Stable release
--------------

To install renodiff, run this command in your terminal:

.. code-block:: console

    $ pip install renodiff

This is the preferred method to install renodiff, as it will always install the most recent stable release. 

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for renodiff can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/honza/renodiff

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/honza/renodiff/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/honza/renodiff
.. _tarball: https://github.com/honza/renodiff/tarball/master
